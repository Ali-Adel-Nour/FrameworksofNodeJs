//Creating Configuarble middleware


function setup(options){
  //Setup Logic

  return function(req, res, next){

    //middleware logic
 }

}

 //the type of middleware is used as follows

 app.use(setup({some:'options'}))